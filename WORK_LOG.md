# AI·에이전트 작업 기록

## 1\. 최초 Implementation Plan

* 계획 요약:

`AGENT\\\_GUIDE.md`, `docs/spec.md`, `docs/plan.md`, `.agents/rules/assignment7.md`를 먼저 읽히고,

코드 수정 없이 7개 항목(프로젝트 구조·실행 방법 / TypeScript·상태관리 준비 상태 /

spec 대비 차이 / 3개 마일스톤 계획 / 예상 변경 파일 / 필요 패키지·설정 / build·검증 방법)을

Implementation Plan으로 요청하기



에이전트가 제시한 계획: Redux Toolkit 기반 노트 CRUD를

기반 구조 → 핵심 기능 → 검증·문서화 3개 마일스톤으로 구현.



* 사람이 수정한 내용:

최초 Plan을 검토한 결과 4가지 문제를 발견하고 수정을 요청했음.



| 항목 | AI 최초 계획 | 사람의 수정 요청 | 이유 |

|---|---|---|---|

| 작업 범위 | 상위 폴더를 워크스페이스로 인식 (프로젝트 루트를 하위 경로로 참조) | 프로젝트 루트를 워크스페이스로 다시 열고 그 밖 접근 금지 | 강의자료·과제 안내 파일까지 시야에 들어와 git 저장소 범위와 diff 검토 범위가 어긋남 |

| 문서 파일 | `README.md`, `WORK\\\_LOG.md`, `docs/plan.md`를 에이전트가 수정 | 문서 8개를 변경 대상에서 제외, 사람이 직접 작성 | 사람의 검토 기록을 에이전트가 작성하면 검토 증거로서 의미가 없음 |

| TypeScript 규칙 | 설정이 켜져 있다는 사실만 언급 | `verbatimModuleSyntax`(타입은 `import type`), `erasableSyntaxOnly`(enum·namespace 금지), `noUnusedLocals` 를 Plan에 명시 | 사전에 못 박지 않으면 구현 단계에서 빌드 오류로 시간이 소모됨 |

| 폼 모드 정책 | "선택된 노트 유무로 모드 전환" 또는 "폼 분리" 중 미확정 | `mode: 'create' \\\\| 'edit'`를 로컬 state로 두고 버튼으로 명시 전환 | 선택 여부에 자동 의존하면 노트를 선택한 상태에서 새 노트를 만들 수 없게 됨 |



수정된 Plan을 재검토한 뒤 2가지를 추가로 확정했음



\- `updateNote`는 `updatedAt`만 갱신하고 `createdAt`은 불변으로 유지, 존재하지 않는 id면 무동작

\- 편집 중인 노트가 삭제되면 `mode`를 `'create'`로 되돌리고 입력값을 초기화 (없는 id로 dispatch 금지)



두 번째 항목은 스냅샷 설계 때문에 새로 생긴 빈틈이었음.

노트 A를 수정 모드로 열어둔 상태에서 A를 삭제하면, 폼은 여전히 A를 편집 중이므로

저장 시 조용히 실패하고 사용자는 저장됐다고 착각하게 됨.



* 승인 결과:

위 6개 사항이 반영된 것을 확인한 뒤 승인하고, 마일스톤을 다시 세분화해 진행함.



\- 마일스톤 1: 기반 구조

\- 마일스톤 2-A: `notesSlice` reducer만

\- 마일스톤 2-B: 컴포넌트 3개와 App 연결

\- 마일스톤 3: 검증·문서화



마일스톤 2를 2-A/2-B로 쪼갠 이유는, reducer와 UI를 한 번에 받으면

diff가 커져서 로직 오류를 눈으로 검토할 수 없기 때문임

reducer 단계는 화면으로 확인이 불가능하므로 코드를 직접 읽어 검증해야 함.



## 2\. 전체 초안 검토

* 구현된 기능:

\- 노트 목록 / 작성 / 선택·확인 / 수정 / 삭제

\- 노트가 없을 때 빈 상태 표시

\- 빈 제목·공백 제목 저장 차단

\- 작성 모드와 수정 모드의 명시적 분리



* 실제 변경 파일:

신규 생성 (7개)



| 파일 | 역할 |

|---|---|

| `src/features/notes/types.ts` | `Note` 인터페이스 |

| `src/features/notes/notesSlice.ts` | 전역 상태, reducer, selector |

| `src/features/notes/NoteList.tsx` | 노트 목록 |

| `src/features/notes/NoteForm.tsx` | 작성·수정 폼 |

| `src/features/notes/NoteDetail.tsx` | 선택된 노트 상세 |

| `src/app/store.ts` | `configureStore`, `RootState`/`AppDispatch` |

| `src/app/hooks.ts` | 타입 지정 `useAppDispatch`/`useAppSelector` |



수정 (3개)



| 파일 | 변경 내용 |

|---|---|

| `src/main.tsx` | `Provider`로 store 연결 |

| `src/App.tsx` | Starter 안내 카드 제거, 노트 앱 레이아웃으로 교체 |

| `src/index.css` | Starter 전용 스타일 제거, 노트 앱 스타일 추가 |



각 마일스톤 종료 시 `git status`로 변경 파일이 계획과 일치하는지 확인했고,

제외 대상으로 지정한 문서 8개와 `package.json`·`tsconfig\\\*.json`·`vite.config.ts`는

전 과정에서 변경되지 않았음.



* build 결과:

tsc -b \&\& vite build

✓ 27 modules transformed.

✓ built in 117ms



`npm run build` 성공, `npm run lint`(oxlint) 오류 0건.



* 화면 테스트 결과:

빌드·린트는 타입 오류와 코드 품질만 잡고 동작의 정확성은 검증하지 못하므로,

10개 시나리오를 사람이 브라우저에서 직접 확인했음.



| 번호 | 시나리오 | 기대 결과 | 실제 결과 | 통과 |

|---|---|---|---|---|

| 1 | 빈 제목 저장 | 차단 + 에러 메시지 | "제목을 입력해 주세요." 표시, 목록 미변경 | ✅ |

| 2 | 공백만 있는 제목 저장 | 차단 | 동일하게 차단 (`trim()` 동작) | ✅ |

| 3 | 정상 노트 추가 | 목록 반영, 폼 초기화 | 목록 추가, 새 노트 자동 선택, 폼 초기화 | ✅ |

| 4 | 노트 선택 | 상세에 내용 표시 | 정상, 폼 상태는 영향 없음 | ✅ |

| 5 | 선택 상태에서 새 노트 작성 | 작성 가능 | 가능, 선택 강조도 유지됨 | ✅ |

| 6 | 노트 수정 | 해당 노트만 변경, `updatedAt` 갱신 | 10:03:17 → 10:08:10 갱신, 다른 노트 시각 불변 | ✅ |

| 7 | 편집 중 목록에서 다른 노트 클릭 | 폼 편집 대상 유지 | 상세만 전환, 폼은 편집 중 노트 유지 | ✅ |

| 8 | 위 상태에서 저장 | 편집 대상 노트가 저장됨 | 편집 대상만 갱신, 선택된 노트는 불변 | ✅ |

| 9 | 편집 중인 노트 삭제 | 폼이 작성 모드로 복귀 | 모드 복귀 + 입력값 초기화 | ✅ |

| 10 | 선택된 노트 삭제 | 선택 상태 정리 | 상세가 빈 상태로 전환 | ✅ |



전체 삭제 후 빈 상태 문구 복귀 확인, 브라우저 콘솔 오류·`key` 경고 0건.



* 발견한 문제:

7번과 8번을 나눠서 확인한 것이 실질적으로 중요했음.

폼에 편집 대상이 그대로 보이더라도, 저장 시 `selectedNoteId`를 참조하도록 구현돼 있으면

화면상으로는 정상이지만 엉뚱한 노트가 덮어써짐.

표시 유지와 dispatch 대상이 일치하는지는 별도로 확인해야 했음.



## 3\. 수정 작업

|문제|원인|수정 내용|재검증 결과|
|-|-|-|-|
|`updateNote` reducer가 순수하지 않음<br /><br /><br /><br /><br />파일이 프로젝트 밖에 생성됨<br /><br /><br /><br />승인 없이 폴더 삭제 실행<br />|`addNote`는 시각 생성을 `prepare`로 분리했으나, `updateNote`는 reducer 내부에서 `new Date().toISOString()`을 호출<br /><br />프로젝트 경로의 한글 폴더명 유니코드 정규화 방식(NFC/NFD) 차이로, 절대경로 작성 시 실제 디스크 경로와 불일치 → 중복 폴더 생성<br /><br />에이전트가 중복 폴더를 스스로 판단해 삭제<br />|<br />`prepare` 콜백으로 `updatedAt` 생성을 이동, reducer는 전달받은 payload만 사용<br /><br /><br /> 이후 모든 파일 작업을 워크스페이스 기준 상대경로로 지시<br /><br /><br />`package.json` 존재 여부로 삭제 대상이 올바랐는지 사후 검증, 이후 프롬프트에 경로 제약 명시<br />|build 성공, 수정 기능 정상 동작, 같은 파일 내 두 reducer 패턴 일관성 확보<br /><br /><br /><br /> `git status`로 정상 위치 반영 확인, 이후 재발 없음<br /><br />실제 프로젝트 파일 손실 없음 확인<br />|

\### `updateNote` 순수성 문제에 대한 기록



Redux reducer는 같은 입력에 같은 출력을 내야 하는데.

`new Date()`는 호출 시점마다 값이 달라지므로 reducer 내부에서 호출하면 순수성이 깨짐

당장 오류는 없었지만 Redux DevTools에서 액션을 리플레이할 때 결과가 재현되지 않음.



빌드와 린트는 이 문제를 잡지 못했음. 타입도 맞고 문법도 정상이기 때문임.

사람이 코드를 직접 읽어야만 발견되는 종류의 문제였음.



\### 승인 없는 폴더 삭제에 대한 기록



`AGENT\\\_GUIDE.md` 5항은 파일·폴더 삭제를 사람의 승인 대상으로 규정함,

에이전트는 이 규칙이 문서로 주어진 상태에서도 승인 없이 삭제를 실행했음.



삭제 판단 자체는 타당했음.

`HAS\\\_PACKAGE\\\_JSON=True`인 폴더를 남기고 `False`인 폴더를 지웠으므로 결과적으로 옳았음.

그러나 판단이 옳았다는 것과 절차를 지켰다는 것은 별개임.

문서로 규칙을 주는 것만으로는 에이전트의 행동을 보장할 수 없으며,

사후 검증 수단(git 커밋 기준점, `git status`)을 사람이 확보해 두어야 한다는 점을 확인했음.



## 4\. 사람의 최종 판단

* 요구사항 충족 여부:

`docs/spec.md` 5항 수용 기준을 기준으로 확인했음.



기술



\- \[x] 주요 React 파일이 `.tsx`로 작성됨

\- \[x] `Note` 타입 정의됨

\- \[x] Redux Toolkit을 실제로 사용

\- \[x] 노트 목록의 기준 데이터가 store에서 관리됨

\- \[x] `npm run build` 성공

\- \[x] 불필요한 `any`·`@ts-ignore` 없음



기능



\- \[x] 빈 상태 표시

\- \[x] 정상 입력으로 노트 생성

\- \[x] 빈 제목·공백 제목 저장 차단

\- \[x] 노트 선택으로 내용 확인

\- \[x] 선택한 노트만 수정

\- \[x] 선택한 노트만 삭제

\- \[x] 선택된 노트 삭제 시 선택 상태 정리

\- \[x] 다른 노트 데이터가 의도치 않게 변경되지 않음



* 남은 문제:

\- 브라우저를 새로 고치면 노트가 사라진다. 저장 기능은 선택 범위여서 이번에 구현하지 않음.

\- 삭제 시 확인 절차가 없어 실수로 지울 수 있음.

\- 노트 수가 많아질 경우의 검색·정렬이 없음.



* 다음 개선점:

\- LocalStorage 연동 (새로고침 후에도 노트 유지)

\- 삭제 확인 다이얼로그

\- 검색·정렬



\## 이번 과정에서 확인한 것



작업을 마일스톤 단위로 쪼갠 것이 검토 가능성을 결정했음.

전체를 한 번에 받으면 diff가 커져서 사실상 검토가 불가능해지고,

그 상태에서는 승인이 형식적인 절차가 됨.



또한 검증에는 층위가 있음.

빌드는 타입 오류를, 린트는 코드 품질 문제를 기계적으로 전수 검사함.

그러나 두 층을 모두 통과한 코드가 엉뚱한 노트를 수정하거나

없는 노트에 저장을 시도하는 것은 사람이 화면에서 확인해야만 드러남.
