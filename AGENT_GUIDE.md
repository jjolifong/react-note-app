# AGENT_GUIDE.md

## 1. 프로젝트 목표

React와 TypeScript를 이용하여 노트 앱을 구현합니다.
노트 목록의 기준 데이터는 Redux Toolkit 등 상태관리 라이브러리에서 관리합니다.

## 2. 기준 문서

다음 순서로 요구사항을 판단합니다.

1. 공식 과제 안내
2. `docs/spec.md`
3. `docs/plan.md`
4. 현재 프로젝트 코드와 설정
5. Agent의 제안

문서와 코드가 충돌하면 임의로 수정하지 말고 먼저 보고합니다.

## 3. 구현 규칙

- React와 TypeScript 구조를 유지합니다.
- 전체 노트 데이터는 Redux store에서 관리합니다.
- 입력 중인 값과 단순 UI 상태만 local state로 둘 수 있습니다.
- 같은 노트 데이터를 store와 local state에 중복 저장하지 않습니다.
- `any`, `@ts-ignore`로 타입 오류를 숨기지 않습니다.
- 요구사항 밖 기능을 임의로 추가하지 않습니다.
- 기존 코드를 전면 재작성하지 않습니다.

## 4. 최초 작업 방식

최초 구현과 구조 변경은 Planning Mode에서 진행합니다.
코드를 수정하기 전에 다음을 제안합니다.

- 구현 목표
- 예상 변경 파일
- 상태관리 구조
- 필요한 패키지와 명령
- 검증 방법
- 위험과 제외 범위

사람이 Implementation Plan을 확인한 뒤 구현을 시작합니다.

## 5. 승인이 필요한 작업

다음 작업은 먼저 승인을 받습니다.

- 패키지 설치·삭제·대규모 업그레이드
- `package.json`, 잠금 파일, TypeScript·빌드 설정 변경
- 파일이나 폴더 삭제
- 프로젝트 전체 구조 변경
- Git commit·push·branch 삭제
- 배포
- 외부 API·DB·MCP 연결
- 비밀정보 또는 실제 사용자 데이터 사용

## 6. 검증 규칙

구현 후 다음을 확인합니다.

- `npm run build` 성공
- 주요 기능의 실제 화면 동작
- 브라우저 콘솔 오류 여부
- 계획하지 않은 파일 변경 여부
- 불필요한 패키지·설정 변경 여부
- `docs/spec.md` 수용 기준 충족 여부

Walkthrough는 참고 자료이며 최종 판정은 사람이 수행합니다.

## 7. 완료 보고

작업 완료 후 다음을 보고합니다.

- 구현한 기능
- 실제 변경 파일과 이유
- build 결과
- 수동 테스트 결과
- 계획과 달라진 점
- 해결되지 않은 문제
- `docs/plan.md`에 반영할 내용
