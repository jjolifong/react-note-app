# AGENT_GUIDE.md

## 1. 프로젝트 목표

React와 TypeScript를 이용해 노트 앱을 구현함.
노트 목록의 기준 데이터는 Redux Toolkit에서 관리함.

## 2. 기준 문서

다음 순서로 요구사항을 판단함.

1. 공식 과제 안내
2. `docs/spec.md`
3. `docs/plan.md`
4. 현재 프로젝트 코드와 설정
5. Agent의 제안

문서와 코드가 충돌하면 임의로 수정하지 않고 먼저 보고함.

## 3. 구현 규칙

- React와 TypeScript 구조를 유지함
- 전체 노트 데이터는 Redux store에서 관리함
- 입력 중인 값(폼 입력값, `mode`, 편집 대상 id 스냅샷)만 local state로 둠
- 같은 노트 데이터를 store와 local state에 중복 저장하지 않음
- `any`, `@ts-ignore`로 타입 오류를 숨기지 않음
- `verbatimModuleSyntax`가 활성화되어 있으므로 타입만 가져올 때는 `import type` 사용
- `erasableSyntaxOnly`가 활성화되어 있으므로 `enum`, `namespace`, 생성자 파라미터 프로퍼티 사용 금지
- 요구사항 밖 기능을 임의로 추가하지 않음
- 기존 코드를 전면 재작성하지 않음

## 4. 작업 방식

최초 구현과 구조 변경은 Implementation Plan을 먼저 제안하고 승인받은 뒤 진행함.
계획에는 다음을 포함함.

- 구현 목표
- 예상 변경 파일
- 상태관리 구조
- 필요한 패키지와 명령
- 검증 방법
- 위험과 제외 범위

기능 범위가 큰 경우, 한 번에 전체를 구현하지 않고 아래 단위로 나눠 진행함.

- reducer(로직) 단계와 컴포넌트(UI) 단계를 분리
- 각 단계마다 build 확인과 `git status` 대조를 거친 뒤 다음 단계로 진행

이렇게 나누는 이유는, reducer 단계는 화면으로 확인이 불가능해 코드를 직접 읽어 검증해야 하고,
로직과 UI를 함께 받으면 diff가 커져 문제를 눈으로 검토하기 어렵기 때문임.

## 5. 승인이 필요한 작업

다음 작업은 먼저 승인을 받음.

- 패키지 설치·삭제·대규모 업그레이드
- `package.json`, 잠금 파일, TypeScript·빌드 설정 변경
- **파일이나 폴더 삭제**
- 프로젝트 전체 구조 변경
- Git commit·push·branch 삭제
- 배포
- 외부 API·DB·MCP 연결
- 비밀정보 또는 실제 사용자 데이터 사용
- 워크스페이스(프로젝트 루트) 밖 파일 접근

## 6. 검증 규칙

구현 후 다음을 확인함.

- `npm run build` 성공
- 주요 기능의 실제 화면 동작 (사람이 브라우저에서 직접 확인)
- 브라우저 콘솔 오류 여부
- 계획하지 않은 파일 변경 여부 (`git status`로 대조)
- 불필요한 패키지·설정 변경 여부
- `docs/spec.md` 수용 기준 충족 여부

Walkthrough는 참고 자료이며 최종 판정은 사람이 수행함.
빌드·린트가 통과해도 로직 오류(예: 잘못된 대상에 저장)는 잡히지 않으므로,
화면에서의 직접 확인은 생략하지 않음.

## 7. 완료 보고

작업 완료 후 다음을 보고함.

- 구현한 기능
- 실제 변경 파일과 이유
- build 결과
- 수동 테스트 결과
- 계획과 달라진 점
- 해결되지 않은 문제
- `docs/plan.md`에 반영할 내용